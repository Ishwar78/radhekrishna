# Fly.io configuration for Vasstra Backend API
app = "vasstra-backend"
primary_region = "iad"

[build]
  builder = "heroku"

[env]
  PORT = "5000"
  MONGODB_URI = "mongodb+srv://sharmaishwar970:ISHWAR123@cluster0.b73q6ph.mongodb.net/Vastra"
  JWT_SECRET = "vasstra_jwt_secret_key_change_in_production_2024"
  NODE_ENV = "production"

[processes]
  app = "node index.js"

[http_service]
  internal_port = 5000
  force_https = false
  auto_stop_machines = true
  auto_start_machines = true
  min_machines_running = 1

[[vm]]
  cpu_kind = "shared"
  cpus = 1
  memory_mb = 256
